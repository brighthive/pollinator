DIRECTORY=${AIRFLOW_HOME:-{{ environment.airflow_home }}}
RETENTION=${BRIGHTHIVE__AIRFLOW__WORKER_LOG_RETENTION_DAYS:-15}
echo "Trimming worker logs to ${RETENTION} days."
find "${DIRECTORY}"/logs -mtime +"${RETENTION}" -name '*.log' -delete